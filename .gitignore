# ── Eclipse workspace metadata ──
.metadata/

# ── Claude / local agent state ──
.claude/

# ── Large third-party datasets (not redistributable; fetch via data/fetch_kaggle.sh) ──
data/raw/

# ── Build outputs ──
**/build/
**/bin/
**/out/
**/target/
.gradle/
!gradle/wrapper/gradle-wrapper.jar

# ── IDE files ──
.project
.classpath
.settings/
.factorypath
.apt_generated
.springBeans
.sts4-cache
.idea/
*.iml
*.iws
*.ipr
.vscode/

# ── OS noise ──
.DS_Store
Thumbs.db

# ── Runtime artifacts (demo-bank SQLite store) ──
*.db
*.db-journal
*.sqlite

# ── Local agent/workflow scratch (never committed) ──
.diakrisis-*.workflow.js
