server:
  port: 8080

spring:
  application:
    name: bank-app
  threads:
    virtual:
      enabled: true
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null

diakrisis:
  jwt:
    # Override in production via DIAKRISIS_JWT_SECRET; must be >= 32 bytes for HS256.
    secret: ${DIAKRISIS_JWT_SECRET:diakrisis-local-development-secret-key-change-me}
  decision-service:
    base-url: ${DIAKRISIS_DECISION_SERVICE_BASE_URL:http://localhost:8081}
  dynamo:
    endpoint: ${DIAKRISIS_DYNAMO_ENDPOINT:http://localhost:8000}
    region: us-east-1
    auto-create: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
