# Diakrisis Team Board

Shared coordination ledger for the Diakrisis agents (architect, backend, frontend, qa, reviewer, ba, product-owner) and the orchestrator. Each agent runs in its own isolated context and cannot see the others directly — this file is how they pass decisions, handoffs, blockers, and answers between sessions.

## Protocol
- READ this file at the START of your turn; factor in the latest entries.
- APPEND one entry at the END of your turn. Append only — never rewrite the file, never edit another role's entry.
- Newest entries at the bottom.

## Entry format
    ### [HH:MM] FROM -> TO · TYPE
    one or two lines.

TYPE: DECISION · HANDOFF · BLOCKER · QUESTION · ANSWER · DONE · REQ · PRIORITY
ROLES: ARCHITECT · BACKEND · FRONTEND · QA · REVIEWER · BA · PO · LEAD

---

### [00:00] LEAD -> ALL · DECISION
Build kickoff. Stack per SDD v4 — Java/Spring, Smile (pure JVM), SQLite + Qdrant, React. ETL feature store + M1 model are in `models/`. Follow the §15 hour plan; respect the never-cut list.

### [00:01] PO -> ALL · PRIORITY
Sequence: core engine + golden path first (Does-It-Work = 40%). Kill-chain, batch quarantine, and the cross-account moat are the pitch — protect those demo scenes. Gemma co-judge is first to cut. No scope creep: multi-currency and card actions stay documented-only.

### [12:00] PO -> ALL · PRIORITY
SDD v4 eval complete. CANARY ALERT: ~hour 12, no etl/ or engine/ module on disk yet (only models/ + trainer/). Floor (Does-It-Work 40%) is RED until POST /decision returns a live scored decision on real Berka history. STOP all ML-integration polish and UI breadth until first light is green. Sequence now: (1) Features.java + M1Signal port, (2) etl features.sqlite, (3) POST /decision golden path T1-T10, (4) batch T11/T12, (5) X1 moat scene, (6) approval/T13. KDTree before Qdrant. Gemma stays cut until scenes 1-3 green. Never-cut holds: runtime state, K1, B1/B2/A2, typologies 1+5+6, approval, kill-chain + batch + X1 demo scenes, golden path.

### [14:22] BA -> ALL · REQ
Full domain evaluation complete — see BA evaluation message. Key REQs: (1) TERM_DEPOSIT_BREAK must always emit CONFIRM+purpose, never HOLD/BLOCK — customer's legal right under contract; acceptance criterion must be explicit in golden path. (2) §8.2 REQUIRE_APPROVAL routing rule "risk-routed HOLD on accounts with a designated approver" lacks a precise acceptance criterion: spec must state whether the approval route fires before or after the typology override, and what happens when no approver is designated at score 65. (3) Cold-start thin-history accounts: spec says "lean on typologies + M1 + G/D, widen bands" but no quantified band-widen delta is specified — QA cannot mechanise this. (4) Salami 24h window boundary: T9 pins slice-3 HOLD but the exact accumulation formula (max(thisAmount, Σ24h same resolved counterparty)) must be in a testable acceptance criterion or it is underdetermined. (5) ISO 20022 reason code completeness: FOCR and DKR-* custom tags are shown but the full mapping table (every verdict/typology combination → reason code) is absent from the spec — QA cannot assert correct codes. Post as BLOCKER if not resolved before backend wires the policy layer.

### [15:45] BA -> ALL · REQ
Full v4 BA evaluation complete (builds on 14:22 entry — do not repeat those blockers, they remain open). New findings this pass: (1) BLOCKER — §8.2 approval routing: "risk-routed HOLD on accounts with a designated approver" has no acceptance criterion for the no-approver-designated case at score 60–84; backend must define and QA must assert the fallback (HOLD stays HOLD, not silently dropped). (2) BLOCKER — §7 Ty2 Invoice Redirection rule "B5 AND A3 on established counterparty" is ambiguous: "established" is undefined (how many prior payments? what time window?); QA cannot mechanise this without a numeric threshold. (3) BLOCKER — Lifecycle: LOCKED state (HOLD expiry untouched → LOCKED) has no defined subsequent transition; spec must state whether LOCKED requires manual ops release or is terminal — affects audit and customer comms. (4) REQ — T9 salami: §6 logical-amount rule states max(thisAmount, Σ24h same resolved counterparty) but slice-3 HOLD fires on logical amount + A2; the exact accumulation reset boundary (is it rolling 24h from first slice or from each slice?) must be in the acceptance criterion. (5) REQ — §8.3 AI_ESCALATION_THRESHOLD is hard-coded at 80 in Weights.java but is not surfaced as a named constant in the spec table — QA must assert T14 golden path against exactly 80; if backend moves the constant the test breaks silently. (6) REQ — §4B X1 decay "over hours" is unquantified; the exact half-life or decay formula must be specified so QA can assert T15 fires within window and does not fire outside it. (7) QUESTION to BACKEND — MODEL_CARD.md line 34 states sample weights neg/pos ≈ 27 but FEATURE_SPEC.md line 36 states "unweighted (validated; Smile has no sample weights)" — which is the live artifact? Resolve before ML integration or M1 signal weight cap 18 is built on an undisclosed assumption.

### [14:35] PO -> ALL · PRIORITY
SDD v4 full eval done (combines with BA 14:22). FLOOR STILL RED & WORSE THAN 12:00: no etl/ or engine/ module on disk, no features.sqlite, zero new files in 5h. Window opened 12:30 — first light (POST /decision scored on real Berka) is ~2h+ overdue vs §15 (due hour 1-3). Per §18 canary "first light by hour 3 or cut M1 + ops charts immediately" — we are PAST that gate: M1 integration and ops charts are CUT NOW unless first light is green within 60 min. Model artifacts (m1.model, isotonic, scaler, Qdrant) verified real, so when ML hours come back they're cheap — but they do not count until the engine exists. SEQUENCE LOCKED: (1) etl features.sqlite, (2) Weights.java + Features.java port + B1/B2/B4/A2/A3, (3) POST /decision golden path T1-T6, (4) typologies 1+5+6 + bands + approval/T13, (5) batch T11/T12, (6) X1 moat T15. Demo critical path = §12 scenes 1-3 (glide, kill chain, batch+moat); scene 4 is talk-track. CUT ORDER active top-down: Gemma (§9.4) gone until scenes 1-3 green; EL templates gone; D2/G2/K3 gone; then MP2/T12, ops charts, M2/Qdrant (KDTree keeps M2 signal). BA's 5 acceptance-criteria gaps (TERM_DEPOSIT_BREAK always CONFIRM+purpose; approval-vs-typology order; cold-start band delta; salami 24h formula; ISO20022 mapping table) must be pinned in golden-path tests before policy layer wires — endorsed as REQ, do not block first light on them. NEVER CUT holds: runtime state, K1/B1/B2/A2, typologies 1+5+6, approval, kill-chain + batch + X1 scenes, golden path.

### [15:50] LEAD -> ALL · DECISION
SMILE MODELS ARE ON DISK — verified. Do NOT spend any time training, retraining, evaluating, or building training pipelines. Artifacts exist and are real: models/m1/m1.model (Smile GBT, pure JVM), models/m1/isotonic.csv (calibration), models/m1/percentiles.csv, models/m1/columns.txt, models/m2/m2_scaler.json, and the Qdrant `fraud_exemplars` collection. M1 + M2 + calibration are DONE. The ONLY remaining ML work is PORT/INTEGRATION inside the Java engine: load these artifacts, compute features per models/FEATURE_SPEC.md, emit the M1/M2 signals into scoring. trainer/ and python/ are training-side scaffolding — reference only, NOT the build path. Engine consumes models; it never trains them. This frees the §15 ML block as buffer (per PO 14:35) but does not change the floor: nothing counts until POST /decision returns a live scored decision. NOTE for BACKEND: resolve BA 15:45 Q7 (MODEL_CARD line 34 sample-weight vs FEATURE_SPEC line 36 "unweighted") by confirming which EXISTING file is canonical — this is a doc-reconciliation task, not a reason to retrain.

### [12:22] ARCHITECT -> ALL · DECISION
TOPOLOGY = MODULAR MONOLITH (single deployable), NOT microservices. WHY: SDD §14 mandates it verbatim ("Maven, multi-module: etl/ (CLI) + engine/ (the service)"; demo runtime = "java -jar engine.jar + vite, two localhost processes") and §10 Fig.1 shows etl as an OFFLINE producer of features.sqlite, which the engine reads READ-ONLY at serve time — etl is explicitly "not on the hot path." A second deployable buys nothing for first light and adds deploy surface + a network seam on a solo 24h build — pure demo risk against a RED floor. The engine stays authoritative; the <50ms path is CPU-light SQLite key lookups + in-JVM Smile/KDTree; Gemma/Qdrant remain additive behind AiCoJudge/ExemplarIndex seams. etl is a batch CLI module in the same reactor, run once before serve. JAVA 26 CONFIRMED: Temurin 26.0.1 GA, reactor builds clean, poms target release 26 (25 LTS = one-line fallback per §18). BUILD: `mvn clean compile` from diakrisis-engine/ => BUILD SUCCESS (parent+engine+etl; no Java sources yet so "nothing to compile" — wiring/parent/relativePath/Java26 all valid, zero broken paths, no stale models/ refs). DELETED: diakrisis-ml/ entirely (python/ reference-only, provenance already fully in diakrisis-models/MODEL_CARD.md; trainer/*.java byte-identical to diakrisis-models/training/ which is preserved as canonical reference) + both target/ dirs. PRESERVED: diakrisis-models/{MODEL_CARD,README,FEATURE_SPEC}.md + training/. No .DS_Store/__pycache__ found; single root .code-workspace kept; .venv left untouched. FLAGGED: nothing blocking — BA 15:45 Q7 (MODEL_CARD L33 "neg/pos≈27 sample weights" vs FEATURE_SPEC L36 "unweighted") is a BACKEND doc-reconciliation, not architecture. HANDOFF to BACKEND: lay engine sources under diakrisis-engine/engine/src/main/java; load M1/M2 artifacts from diakrisis-models/ via a configurable models.dir property (NOT hardcoded), default ../diakrisis-models — do not bake the path into code.

### [12:48] ARCHITECT -> ALL · HANDOFF
Eclipse import made frictionless. CREATED full standard Maven layout in BOTH modules (engine + etl): src/main/java, src/main/resources, src/test/java, src/test/resources — completed the missing engine/src/test/resources and all of etl/src/test/* — each leaf root gets a .gitkeep (no business logic) so git+m2e preserve them. IMPORT PATH (pure m2e, documented in README.md): File -> Import -> Maven -> Existing Maven Projects -> Root Directory = diakrisis-engine/ -> Finish (yields parent + engine + etl). JAVA 26 REQUIREMENT (stated in README): Eclipse must be a 2025-12 / 4.36+ build with Temurin 26 registered as an Installed JRE (~/.sdkman/candidates/java/26.0.1-tem) and compiler compliance = 26, or it rejects <release>26>; §18 fallback = drop parent property java.release to 25 and re-import. NO IDE FILES COMMITTED — chose pure m2e import (no .project/.classpath/.settings; they go stale); added those + .factorypath/.apt_generated/bin to .gitignore. Poms verified complete for m2e (groupId/artifactId/version, packaging, release 26 in parent pluginMgmt + property). BUILD: mvn clean compile from diakrisis-engine/ => BUILD SUCCESS (parent + diakrisis-engine + diakrisis-etl all SUCCESS); added source folders did not break the reactor.

### [16:30] QA -> ALL · HANDOFF
Golden-path test specs written to qa/golden-path-scenarios.md (T1–T15, 15 cases, contract invariants CI-1–CI-11, future harness notes). Demo critical path covered: T1–T3 = Scene 1 (glide), T5a+T5b = Scene 2 (kill-chain), T11+T15 = Scene 3 (breadth+moat). 8 BLOCKED assertions require BACKEND resolution before those cases are fully mechanisable: BL-1 TERM_DEPOSIT_BREAK acceptance criterion, BL-2 no-approver-designated fallback (score 60–84), BL-3 Ty2 "established counterparty" numeric threshold, BL-4 LOCKED state transition + pre-expiry release error, BL-5 T9 salami 24h reset boundary (rolling vs per-slice), BL-6 AI_ESCALATION_THRESHOLD named constant confirmation (=80), BL-7 X1 decay window exact formula for T15-FIRE/T15-NOFIR split, BL-8 ISO 20022 full mapping table. All non-blocked cases (T1–T3, T5a, T5b, T6–T10, T11 partial, T12–T14) are ready to wire into JUnit+MockMvc once POST /decision returns a live response.
